package ejemplos.tema2;


/**
* GeneradorDeString(n) allows to generate Strings which have
* the same initial n characters.
* Usage example to create two Strings s1 and s2 that share the
* first 10 characters:
* GeneradorDeString g = new GeneradorDeString(10);
* String s1 = g.generar();
* String s2 = g.generar();
*
* @author (EDA)
* @version (Year 2017-2018)
*/
public class GeneradorDeString {
private int numIg; // Number of equal initial characters of the Strings
private String base; // Initial subString equal for all generated Strings
/**
* Creats a GeneradorDeString object whose n first characters
* are equal.
*/
public GeneradorDeString(int n) {  }
/**
* Returns a String with identical n initial characters
* although randomly chosen.
*/
public String generar() { return "";

}
}
